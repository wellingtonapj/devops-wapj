- name: Criação de VPC
    ec2_vpc_net:
      region: "{{ aws_region }}"
      name: "{{ vpc_name }}" 
      cidr_block: "{{ vpcCidrBlock }}"      
      dns_support: yes
      dns_hostnames: yes
      tenancy: default
      tags:
        Name: "{{ vpc_name }}"
    register: vpc_result
